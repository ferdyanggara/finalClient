class Product {
    constructor(id, ownerId, title, imageUrl, description, price, type) {
        this.id = id
        this.ownerId = ownerId
        this.imageUrl = imageUrl
        this.title = title
        this.description = description
        this.price = price
        this.type = type
    }
}

export default Product
